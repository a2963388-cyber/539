# 回測事前註冊（BACKTEST_REGISTRY）

**註冊日：2026-08-16　拍板：鈞洋**

> 本檔在**跑第一次回測之前**寫定並提交。所有假設、對照線、檢定項目、判準、
> 以及「什麼結果對應什麼行動」皆於此固定。跑完之後**不得新增檢定、不得更改判準、
> 不得挑選對自己有利的子集呈現**。
>
> 制定理由：不事前註冊的回測，等於跑完再挑好看的講 —— 這正是 2026-08-09
> 砍掉舊 10 策略制的原因。回測本身若不受同一套紀律約束，就沒有資格審判選號制度。

## 📌 修訂紀錄

註冊後的修改一律列於此，寫明改了什麼、為什麼、以及**是否影響結論方向**。

| 日期 | 修訂 | 原因 | 影響 |
|---|---|---|---|
| 2026-08-16 a | 六合兌獎口徑改為「主 6 ＋ 特別號 = 7 碼」 | 誤解鈞洋的話 | **已撤銷，見下列 b** |
| 2026-08-16 b | **撤回上列**，並改為**雙口徑**：<br>・**兌獎**＝主 6 碼（特碼不計，維持原樣）→ T1/T2/T5<br>・**出現**＝主 6 ＋ 特碼 = 7 碼 → T3 排除區命中判定 | 鈞洋澄清：「特碼必須有算，是要給**不出牌方案**知道的」「**中獎方面特碼是不用計**」 | 兌獎相關數字全部回到原值（理論 1.4694、三碼全中 11 次）；T3 的 null 改用 7/49＝14.29%。**539／F5 完全不受影響**（已回歸驗證）。**五項檢定的顯著性結論方向全部不變** |
| 2026-08-16 c | 另加一項非註冊的附帶實測（工具與結果留在本機，不進版控） | 鈞洋另有提問 | 獨立於五項主檢定，不影響 Holm 校正的 30 次計數 |

⚠️ 上列修訂皆為**更正事實錯誤或回應新問題**，不是為了迎合結果而改判準 ——
判準（α、檢定方法、Holm 校正、預先承諾的行動）一字未動。
⚠️ 修訂 a→b 是我理解錯誤造成的來回，原樣保留不刪，供審計。

---

## 0. 誠實界定：本回測能與不能回答什麼

**不能回答**：「這樣選會不會比較容易中」。
選號不改變中獎機率，這是數學事實，回測不可能推翻。
**任何顯示「顯著提高命中率」的結果，第一順位假設是程式有 bug，不是發現了規律。**

**能回答**：過濾規則有沒有**副作用**、排除區到底有沒有作用、號碼分佈有沒有被系統性扭曲。
即：本回測是**除錯與品管工具**，不是績效證明。

---

## 1. 資料

| 彩種 | 期數 | 期號範圍 | 來源 |
|---|---|---|---|
| 539 | 5942 | 96001 ~ 115198 | `history_539.json`（官方 API）＋ `BASE_REC` |
| f5 | 6802 | 5169 ~ 11970 | `history_f5.json`（lottolyzer）＋ `BASE_REC` |
| m6 | 2587 | 8001 ~ 26089 | `history_m6.json`（lottolyzer）＋ `BASE_REC` |

- 整備腳本：`build_backtest_data.py`（唯讀來源，不動產線檔）
- 完整性檢查 C1/C2/C3 全過：兩來源在 172/223/310 期重疊上**號碼零衝突**，期號零缺口
- **warmup = 500 期**：前 500 期只餵資料不評估（平坦度需 `at_risk` 樣本，過薄無意義）
- 可評估期數：539 **5441**、f5 **6301**、m6 **2086**
- 🔴 六合彩**雙口徑**（2026-08-16 鈞洋澄清，見上方修訂紀錄 b）：
  | 用途 | 口徑 | 單碼率 | 用在 |
  |---|---|---|---|
  | **兌獎** | 主 6 碼，特碼**不計** | 6/49 = 12.24% | T1／T2／T5、三碼全中 |
  | **出現** | 主 6 ＋ 特碼 = 7 碼 | 7/49 = 14.29% | T3 排除區命中、N 不出判定 |

  ※ 兌獎口徑與產線 `build_log_entries` 一致，**維持不動**。
  ※ 排除區／平坦度的 `dist`／`gaps` 仍只吃主 6 碼（`GAMES["m6"]["draw"]=6`），
    是否改含特碼待拍板 —— `backtest_notout.py` 實測顯示改了也沒有改善。

### 歷史窗口

每期一律使用「**該期之前的全部可用歷史**」（累積窗，只增不減），
理由：這是產線 `all_records = actually_new + existing`（`539_fetch.py:409`）的真實行為，
也是它的必然去向。回測同時記錄每期窗長，供事後分段分析（短窗期 vs 長窗期）。

---

## 2. 對照線（五條）

| 代號 | 定義 | 用來隔離什麼 |
|---|---|---|
| **S3** | sfg-v3 ＋ fpx-v3（現行制度） | 現況基準 |
| **S4** | sfg-v3 ＋ **fpx-v4**（見 §3） | 修正漂移後的版本 |
| **B2** | sfg-v3，`excluded = ∅` | S3−B2 ＝ fpx-v3 淨效果；S4−B2 ＝ fpx-v4 淨效果 |
| **B1** | **同種子、同 Lehmer 數列，關閉全部過濾規則**：自 s0 連續取號，跳過重複，湊滿 12 個不重複號，依序切成 A/B/C/D | **S−B1 ＝ 過濾規則的淨效果**。共享 PRNG，把亂數本身的影響完全消掉 |
| **B0** | 理論基準：12 碼取自 pool 的超幾何期望 | 絕對基準 |

B1 是本回測的核心對照。因與 S 共享同一條數列、對同一期開獎結果兌獎，
可做**同期配對比較**，變異遠低於各自對隨機基準比。

---

## 3. fpx-v4 規格（本次一併註冊，僅供回測評估，**尚未進產線**）

**動機（結構性，非因近期未中）**：`ATRISK_MIN = 20` 是**絕對**門檻。
歷史越長，深層柱子越容易達標；而深層樣本薄、h 波動大，易產生極端低值被選為排除柱，
但深度 40+ 的柱子上幾乎不會有號碼 ⇒ **排除區趨向永遠為空，規則無聲失效**。

實測（2026-08-16）：排除區均值隨窗長單調衰減
539 窗100 = 2.03 顆 → 現況(198) = 0.40 → 全歷史 = 0.05 顆（95.3% 為空）；
f5 全歷史 **99.0% 為空**；m6 全歷史 86.7% 為空。
且 `pick_engine.py:48-50` 註冊的 1.44/1.75/0.88 顆，經復刻證實是
**平均窗長僅 119/147/181 期**的回放結果 —— 產線實際窗口從未處在該狀態。

**v4 唯一變更**：

```
舊 (v3)：  at_risk < ATRISK_MIN(20)                → break
新 (v4)：  at_risk < max(ATRISK_MIN, 0.05 × 總樣本) → break
```

其餘（`EXCL_BARS = 2`、h 算式、排序、回補順序）**完全不變**。

**效果驗證**：可達搜尋深度由「隨窗長漂移」變為「穩定」

| 彩種 | v3 絕對門檻（窗150→全歷史） | v4 相對門檻（同上） |
|---|---|---|
| 539 | 24 → 26 → 31 → 40 → **53** | 21 → 20 → 20 → 21 → **22** |
| f5 | 26 → 28 → 33 → 41 → **55** | 20 → 20 → 20 → 21 → **22** |
| m6 | 28 → 30 → 33 → 46 → **50** | 22 → 23 → 23 → 23 → **22** |

---

## 4. 檢定清單與判準

所有檢定 **α = 0.05**，採 **Holm-Bonferroni** 校正（15 次檢定：5 項 × 3 彩種），
報告同時列出**原始 p 值**與**校正後結論**。

| # | 檢定 | 方法 | 虛無假設 |
|---|---|---|---|
| **T1** | S3 vs B1 總命中碼數 | 同期配對 Wilcoxon 符號秩 | 過濾規則無淨效果 |
| **T2** | S3 vs B2、S4 vs B2 總命中碼數 | 同期配對 Wilcoxon | 排除區無淨效果 |
| **T3** | 排除區命中率 vs `draw/pool_max` | **合併二項檢定** | 等於理論值 |
| **T4** | 各號碼入選次數均勻度 | 卡方適合度（df = pool_max−1） | 均勻 |
| **T5** | 每組命中數 0/1/2/3 分佈 | 卡方 vs 超幾何理論 | 符合理論 |

### 🔴 兩個必須做對、否則結論不可信之處

1. **T3 不得取各期命中率的平均。**
   排除區每期大小不同（0～多顆，且多數期為 0 顆），逐期算比率再平均會被 0 顆期數污染成無意義的數。
   **必須合併全期的「排除號碼總數」與「其中開出總數」做單一二項檢定。**

2. **T4 的期望值不是均勻分佈的 1/pool_max。**
   sfg 每期固定選 12 碼，故期望入選次數 = 期數 × 12 / pool_max。
   卡方要對這個期望算，不是對開獎頻率算。

### 效力分析（先算，避免把「測不出來」講成「沒有差」）

| 彩種 | 可評估期數 | 期望總命中碼數 | SD | **可偵測相對效果（2 SD）** |
|---|---|---|---|---|
| 539 | 5441 | 8371 | 85.4 | **± 2.04%** |
| f5 | 6301 | 9694 | 91.9 | **± 1.90%** |
| m6 | 2086 | 3576 | 55.4 | **± 3.10%** |

（六合數值為 2026-08-16 兌獎口徑更正後重算，p₀ = 7/49；更正前誤用 6/49 得 ±3.39%。）

⚠️ **因此，若 p > 0.05，正確講法是「排除了大於上表幅度的效果」，不是「證明沒有差異」。**
此句必須原文印在最終報告上。

---

## 5. 預先承諾的行動

| 結果 | 行動 |
|---|---|
| T1 顯著**負向**（規則扣分） | 定位是哪條規則，註冊 sfg-v4 移除或放寬 |
| T1 不顯著 | ✅ 規則無副作用，維持現制，把數據寫進 `pick_engine.py` 註解 |
| T1 顯著**正向** | 🚨 **不改制度**，先假設程式有 bug，全面複查後才談結論 |
| T2/T3 顯示 v4 明顯優於 v3 | 註冊 fpx-v4 進產線，**只往前生效不重算歷史** |
| T2/T3 顯示 v3 與 v4 皆與 B2 無異 | 認定排除區無作用，評估直接廢除以簡化制度 → **✅ 2026-08-17 已執行，見下方** |
| T4 顯著 | 查是否 G2／G3 造成，評估是否傷及反熱門目標 |
| T5 顯著 | 檢查兌獎口徑或抽取程序是否有實作錯誤 |

---

## 5.1 ✅ 執行結果：sfg-v4 廢除 fpx（2026-08-17 鈞洋拍板）

依 §5「T3 全不顯著 → 認定排除區無作用 → 評估直接廢除」，鈞洋當日拍板**廢除**。

- `pick_engine.gen_pending_core` 不再呼叫 `flatness_exclude`，候選池＝全池
- 版本升 **sfg-v4**，輸出不再含 `excluded` / `exclAlgo` / `exclDepths`
- `flatness_exclude()` **保留但退役**：歷史 PICKLOG 需要它才能解讀，
  回測對照組也還在用。產線路徑不得再接回
- selftest 新增 v4 閘門：產線輸出若又出現排除區欄位或版號不符，直接 FAIL
- 舊 PICKLOG 原樣保留供審計，改版只往前生效

### 這次順手抓到的三個既有缺陷（都是「寫死版號」）

改版時發現 v3 早就踩到但沒人發現的坑：

| 位置 | 原本 | 後果 |
|---|---|---|
| `verify_consistency.js:23` | `if(v!==2&&v!==3) return null` | v4 會**靜默跳過全部結構驗規**，保護消失且不會有人發現 |
| `verify_consistency.js:129` | `if(recompute&&v===2)` | 🔴 **v3 的 pending 從未被重算比對過** —— 它顯示的 ✅ 只是因為整段被跳過 |
| 三個 HTML ×3 處 | `l.v===2` / `P.v===2` | v3/v4 的 PICKLOG 條目會被標成「舊制」並排除在新制統計外。目前尚無 v3 條目所以未發作，**下次開獎就會** |

全部改為下限判定（`v>=2`）。教訓：**版號判定一律寫成下限，寫死等值就是在賭「未來的人記得回來加數字」**。

另加**跨版本判定**：pending 的 algo 與現行引擎不同時，顯示「改版只往前生效，跳過重算」
而非報 FAIL —— 否則每次改版當天 verify 都會紅一片，而誤報比漏報更傷。
但加了時效保護：pending 超過 7 天未更新仍算真故障（避免「跳過」變成永久盲點）。

## 6. 預期結果（先寫下，事後不得改口）

**最可能是三彩種、五項檢定幾乎全部不顯著** —— 一如 2026-08-16 對連莊率的實測（p = 0.46/0.14/0.62）。
那不是白做：那正是「規則沒有偷偷扣分」的證據，且從此有可複驗的數據撐著。

若出現顯著，預期方向為**負向**（過濾規則沒有理由讓命中率變好）。
唯一已預期會顯著的是 **T2/T3 中 v3 與 v4 的排除區規模差異** —— 那是規格差異，不是機率發現。

---

## 7. 產出

```
BACKTEST_REGISTRY.md        本檔（先提交，後跑）
build_backtest_data.py      資料整備 ✅ 已完成
backtest.py                 回測主程式
backtest_v3_results.json    原始數據（可複驗）
backtest_v3_report.html     報告
attic/                      舊的 backtest_report.html、backtest_v2_results.json（舊 10 策略制）
```
